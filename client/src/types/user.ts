/* This code snippet is defining an interface named `User` in TypeScript. An interface in TypeScript is
a way to define the shape of an object. In this case, the `User` interface has three properties:
`id` of type string, `name` of type string, and `email` of type string. */
export interface User {
  id: string;
  name: string;
  email: string;
}